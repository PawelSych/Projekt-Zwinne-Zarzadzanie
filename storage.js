/* storage.js
   Zakres (Osoba 3 - Storage/UX): localStorage (odczyt/zapis), import/export JSON, komunikaty UX.
   Plik zawiera placeholder, aby repo posiadało komplet podstawowych modułów.
*/

(function () {
  window.KanbanStorage = {
    load() {
      return [];
    },
    save(_tasks) {
      // placeholder
    },
  };

  console.log("Storage placeholder ready.");
})();
